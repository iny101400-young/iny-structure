---
name: iny-structure
description: 03단계. 02 가 정한 컨셉을 기준으로 재료를 글감·근거·안 씀 셋으로 가르고, 고른 것만 주제로 엮어 AI 가 읽는 위키로 만든다. 재료를 전부 위키로 만들지 않는다. 문서마다 본인 자료 출처를 달고 안 엮은 것은 개수와 이유를 말한다. 그 위에서 허브와 클러스터를 세우고, 답할 재료가 있는 질문만 만들고, 글 목록 초안과 주소 규칙까지 낸다. 여기서 나온 구조가 04 화면설계부터 06 글 쓰기까지의 뼈대가 된다. 트리거는 "구조 잡기", "구조화", "위키 만들기", "주제 묶기", "글 목록 뽑기", "iny-structure", "03 시작".
---

# 03 STRUCTURE · 구조 잡기

`v1.2`

`louiswang524/llm-knowledge-base` (MIT · Copyright (c) 2026 Louis Wang) 의 위키 구조와
`kb-reflect` · `kb-lint` · `kb-ask` 설계를 바탕으로 고쳐 썼다.
**kb 스킬은 한 개도 설치하지 않는다.** 이유는 [`references/위키규칙.md`](references/위키규칙.md) 7절에 있다.

## ⚑ 실행 순서

건너뛰지 않는다.

| STEP | 무엇 | 멈추나 |
|---|---|---|
| **1** | **안내문을 낸다** (아래 「첫 메시지」 그대로) | ✋ 답할 때까지 |
| **2** | 02 산출물을 읽는다. 없으면 02 부터 하시라고 안내하고 멈춘다 | |
| **3** | 훑기. **화면에 아무것도 안 낸다** | |
| **4** | 클릭 ① 이 묶음들은 누가 만든 건가 | ✋ |
| **5** | **3분류를 다 해놓고** 묶음 단위 표로 보여준다 · 클릭 ② | ✋ |
| **6** | 클릭 ③ 직접 만드신 화면 {N}장을 열어 채울지 | ✋ |
| **7** | 클릭 ④ 허브 두 층 | ✋ |
| **8** | 이미지 채우기 · **위키 짓기** | |
| **9** | 빠진 연결 찾기 · 검사 · 고치기 | |
| **10** | 질문 · 글 목록 초안 · 주소 규칙 · 클릭 ⑤ | ✋ |
| **11** | **문서 셋을 쓰고 `plan.md` 와 04 안내를 한 덩어리로 낸다** | ✋ 마지막 |

**STEP 1 을 건너뛰지 않는다.** "구조 잡기" 라고만 해도 안내문부터 낸다.
설정이나 재료부터 읽고 그 상태를 보고하지 않는다.

## ⚑ 꼬리를 빠뜨리지 않는다

**02 1회차에 STEP 10·11 이 통째로 날아갔다.** `plan.md` 를 보여주고 확인받는 자리에서
대화가 끝났고, 그 뒤에 있던 것이 전부 밀렸다.

> **확인받는 자리 뒤에 뭘 두지 않는다. 거기서 세션이 끝난다.**

그래서 03 은 **마지막 확인을 진짜 마지막에 둔다.**

| | |
|---|---|
| 1 | **STEP 11 이 마지막이다.** 문서 쓰기 · `plan.md` 보여주기 · 04 안내를 **한 덩어리로** 낸다 |
| 2 | 그 덩어리의 **맨 끝**이 "고칠 데 말씀해 주세요" 다. 그 뒤에 아무것도 없다 |
| 3 | **`plan.md` 「다음에 할 일」에 이 스킬이 할 일을 적지 않는다.** 거기 적힌 건 사람이 나중에 할 일이다 |
| 4 | 산출물에 **「나중에」 · 「필요하면」 · 「사용자가 직접」** 을 쓰지 않는다. 아래 |

## ⚑ 누가 할 일인지 가른다

**02 v1.1 이 여기서 걸렸다.** 재는 것을 스킬이 할 수 있는데 「사용자가 직접」이라고 적었고,
사용자는 그걸 안 했다. 이유가 셋이었다. 누가 하는지 안 적혔고, 어디에 넣는지 안 말했고,
안 해도 되는 건지 몰랐다.

**산출물에 이 세 말이 적히는 자리가 있으면 거기가 안 될 자리다.**

```
사용자가 직접   ·   나중에   ·   필요하면
```

적기 전에 순서대로 본다.

| | |
|---|---|
| 1 | **도구로 되는 일인가.** 되면 내가 한다. 이미지 서술도 `grep` 도 검사도 내가 한다 |
| 2 | 안 되면 **선택이라고 먼저 말한다.** "안 하셔도 됩니다" 를 앞에 놓고 안 하면 뭐가 없어지는지 말한다 |
| 3 | 하면 **넣은 자리를 한 줄로 말한다.** "제가 구조.md 에 넣었습니다. 따로 옮겨 적으실 것 없습니다" |

**손일을 사람에게 넘기지 않는다.** 재료 558개를 사람이 하나씩 판정하는 화면을 만들지 않는다.
**내가 다 갈라놓고 사람은 「이건 아닌데」만 짚는다.**

## ⚑ 지킬 수 없는 약속을 하지 않는다

**「재료를 전부 위키로 만든다」고 말하지 않는다.**

재료 하나마다 요약 문서 하나를 만들면 재료가 1,000개일 때 문서가 1,000장이고
그중 절반은 이미지와 복사본에서 나온다. **그 문서는 원래 재료보다 정보가 적다.**

말을 이렇게 나눠 쓴다. 화면에서도 이 구분을 지킨다.

| | 무엇 | 몇 개까지 |
|---|---|---|
| **훑는다** | 머리말과 글자수만 본다 | 전량 |
| **가른다** | 컨셉 기준으로 글감·근거·안 씀으로 나눈다 | 전량 |
| **엮는다** | 주제로 묶어 위키 문서를 쓴다 | 글감만 |
| **안 엮는다** | 목록에는 남기고 문서로 안 만든다. **개수와 이유를 말한다** | 나머지 |

**장수를 미리 약속하지 않는다.** 몇 장을 만들겠다고 말하지 않는다.
조건 셋을 걸고 다 만든 뒤에 몇 장인지 말한다.

| | |
|---|---|
| 1 | 모든 문서가 어느 허브에 붙어 있다 |
| 2 | 모든 문서에 본인 자료 출처가 붙어 있다 |
| 3 | 본문 없는 재료에서 만든 문서가 0이다 |

**재료는 하나도 안 지운다.** 「안 씀」은 이번에 안 쓴다는 뜻이다. 매번 같이 말한다.

## ⚑ 첫 화면에 내면 안 되는 말

**"재료 {N}개 중 {N}개가 규칙에 걸렸고 개인정보 후보가 {N}건입니다" 같은 말을 첫 화면에 내지 않는다.**
우리한테 필요한 말이지 쓰는 사람한테는 겁만 준다.

설정 읽기, 훑기, 3분류는 **조용히** 한다.
`분류표`, `해시`, `NFC`, `허브` 같은 내부 용어는 설명 없이 쓰지 않는다.
STEP 3 과 STEP 5 앞부분에서 나온 것은 **STEP 5 의 표 한 장으로 요약해서** 낸다.

## ⚑ 출력이 터지지 않게 한다

01 이 716KB 를 화면에 내려다 막혔다. **03 은 위키 목차가 그 자리다.**

| | |
|---|---|
| 목차 | **허브 단위로 접어서** 낸다. 문서 제목을 다 나열하지 않는다 |
| 분류 결과 | **묶음 단위**로 낸다. 파일 목록을 화면에 쏟지 않는다 |
| 검사 결과 | **짚힌 것의 개수와 종류**만. 목록은 파일에 있다 |

## ⚑ 화면에 낼 때 쓰는 말 · 어려운 말을 그대로 안 쓴다

**「허브」 · 「클러스터」 · 「위키」 · 「슬러그」는 우리끼리 쓰는 말이다.**
받아 쓰는 사람은 이 말을 모른다. 모르면 물어보지도 못하고 그냥 안 쓴다.

이 문서 안에서는 그대로 쓴다. 파일 머리말도 그대로다. **화면에만 바꿔 쓴다.**

| 우리 말 | 화면에 낼 말 |
|---|---|
| 허브 | **큰 갈래** |
| 클러스터 | **작은 갈래** |
| 위키 | **주제 문서** · 여러 장일 때 **문서 묶음** |
| 슬러그 | **주소에 쓸 이름** |
| 고아 | **어느 갈래에도 안 붙은 문서** |
| 앵커 | **원문 조각** |
| 머리말 | **문서 맨 위 정보 칸** |
| 3분류 | **글감 · 근거 · 안 씀 셋으로 가르기** |

### 처음 한 번은 풀어서 같이 적는다

그 말이 처음 나오는 자리에서만 괄호로 원래 말을 붙인다. **두 번째부터는 안 붙인다.**

```
주제를 두 층으로 묶었어요.
큰 갈래 안에 작은 갈래가 들어갑니다.
```

### 파일 안은 안 바꾼다

`허브:` · `클러스터:` 는 문서 맨 위에 그대로 쓴다. **04·05·06 이 그 이름으로 읽는다.**
바꾸면 뒤 단계가 못 읽는다. **바꾸는 건 화면에 내는 말뿐이다.**

## ⚑ 오탈자

보내기 전에 한 번 읽는다. 한 글자 틀리면 "대충 만들었네"로 읽히고 그 뒤 신뢰가 통째로 떨어진다.
**아래 「고정 문구」에 적힌 문장은 그대로 복사해서 쓴다.** 즉흥으로 다시 쓰지 않는다.

---

## 이 스킬이 진짜 하는 일

재료를 옮겨 담는 게 아니다. **흩어진 재료를 남이 궁금해하는 순서로 다시 놓는 것**이다.

```
재료는 본인이 쓴 순서로 있다        회고일지 · 수행일지 · 이력서 · 보고서
   ↓
남이 궁금해하는 순서는 그게 아니다
   ↓
"AI 툴 쓸지 말지 뭘 보고 정해요?"  ← 이 하나에 답하려면 재료 세 곳을 뒤져야 한다
   ↓
그 셋을 한자리에 모으고 재명명된 이름을 붙인다   ← 여기가 핵심
   ↓
그 이름이 06 에서 주소가 되고 07 에서 글이 되고 08 에서 검색 결과가 된다
```

위키 한 장이 세 군데로 그대로 간다.

| 어디서 | 위키 한 장이 뭐가 되나 |
|---|---|
| 05 사이트 | 페이지 한 개. 주소 하나 |
| 07 글쓰기 | 글 한 편의 뼈대. 근거가 이미 붙어 있다 |
| AI 가 답할 때 | 「이 사람은 뭘 할 줄 아나」에 꺼내 쓰는 조각 |

**02 가 「지금 AI 는 나를 이렇게 말한다」를 재뒀다.** 대개 검색 결과 0건이다.
**위키 한 장이 나중에 그 검색 결과 한 줄이 된다.**

### 재료를 다 엮지 않는 이유

읽는 쪽은 AI 라 양이 늘어도 된다. **문제는 만드는 쪽이다.**

| | |
|---|---|
| 읽기 | 검색해서 필요한 것만 꺼낸다. 3,000장이든 300장이든 한 번에 몇 장만 연다 |
| 쓰기 | **한 장씩 순서대로 돈다.** 건너뛸 수가 없다 |

그리고 알맹이 없는 문서가 섞이면 꺼내 쓰는 품질이 떨어진다.
얇은 문서와 복사본을 감점하는 건 검색엔진의 오래된 규칙이고 AI 답변엔진도 같은 신호를 쓴다.

> **양이 많아서 문제가 아니라 알맹이 비율이 낮아서 문제다.**

---

## 산출물

**문서 3장.** 여덟 장을 한 번에 쏟지 않는다.

```
{작업폴더}/
├── wiki/
│   ├── index.md              목차. 허브 단위로 접혀 있다
│   └── concepts/             주제마다 1장
└── outputs/03-structure/
    ├── plan.md               ★ 여기부터 본다. 지금 어디고 다음에 뭘 하나
    ├── 구조.md               허브 · 클러스터 · 질문 · 글 목록 초안 · 주소 규칙
    └── 분류.md               글감 / 근거 / 안 씀(이유)
```

기계가 읽는 판은 따로 남는다. **사람이 읽는 문서에 파일 목록을 쏟지 않는다.**

| | |
|---|---|
| `.kb/분류표.tsv` | 파일 한 줄씩 · 분류 칸 · 위키 칸 |
| `.kb/구조지도.json` | 묶음별 집계 · 이미지 갈래 · 개인정보 후보 |
| `.kb/위키검사.json` | 검사 결과 |

### `wiki/sources/` 를 안 만든다

01 이 폴더는 만들어뒀다. **비워 둔다.** `index.md` 에 왜 비었는지 한 줄 적는다.

재료 하나마다 요약 문서 하나를 만드는 자리인데, **그 문서는 원래 재료보다 정보가 적다.**
「이 주제가 어느 재료에서 나왔나」는 개념 문서 안의 **「어디서 나왔나」 표**가 이미 한다.
재료를 거꾸로 찾을 일은 `.kb/분류표.tsv` 의 `위키` 칸이 받는다.

### 안 만드는 것

| | 어디서 |
|---|---|
| 화면설계서 · 디자인 레퍼런스 | 04 DESIGN |
| 계정 아이디 정리 · 사이트 주소 이름 | **02 IDENTIFY** |
| 기계가 읽는 표시의 종류 | **04 DESIGN** |
| 실제 페이지 · 사이트 | **05 BUILD** |
| 글 본문 | **06 PUBLISH.** 03 은 글 목록 초안까지만 |

---

## 절대 규칙

| | |
|---|---|
| 지어내기 | 재료에 없는 일을 문서로 만들지 않는다. **비면 비었다고 말한다** |
| 남의 저작물 | 위키로 안 엮는다. **재료에서 빼지도 않는다.** 뭘 배웠나의 흔적으로는 값이 있다 |
| 폴더 이름 | **폴더 이름으로 주인을 가르지 않는다. 폴더 끝의 사람 이름으로 가른다.** 학원·부트캠프 폴더는 강사 자료와 본인 제출물이 한자리에 있다 |
| 이미지 | **형식이 이미지라고 자동으로 빼지 않는다.** 다른 재료와 똑같이 컨셉 기준으로 가른다 |
| 개인정보 | **문서를 쓰기 전에 막는다.** ★ **재료를 빼는 게 아니라 값을 가린다.** 값은 화면에도 문서에도 기록에도 안 옮긴다 |
| 절대경로 | 산출물에 안 적는다. 사용자 이름이 박혀 있다 |
| 개수 | 위키 장수 · 질문 개수 · 허브 개수를 미리 안 박는다. **다 만들고 나서 말한다** |
| 자동 커밋 | **스킬이 알아서 `git commit` 하지 않는다.** 뭐가 들어갔는지 모른 채 이력이 쌓인다 |
| 어려운 말 | 안 쓴다. 써야 하면 그 자리에서 한 줄로 푼다 |
| 중간에 멈춤 | 지금까지 나온 것만으로 문서를 만든다. **완주를 조건으로 걸지 않는다** |
| 내부 문장 | 이 문서의 규칙을 화면에 읽어주지 않는다 |
| 대시와 불릿 | 화면에 내는 문구에 대시(—)와 하이픈 불릿을 쓰지 않는다. 표나 번호로 쓴다 |
| 경로 | 이 파일에 적어두지 않는다. 설정에서 읽거나 사용자에게 묻는다 |
| 시간 | **숫자로 약속하지 않는다.** 재료 양에 따라 크게 달라진다 |

---

## 대화 원칙

**상담이 아니라 작업이다.** 그런데 결과를 읽을 때는 **재미있어야** 한다.

| ✕ 깎기 | ✕ 치켜세우기 |
|---|---|
| "그건 좀 약한데요" | "우와 대단한데요" |
| "생각보다 적네요" | "충분히 훌륭하세요" |

둘 다 평가다. 대신 **형용사를 빼고 사실과 발견만** 내놓는다.

```
✕  "정말 값진 자료가 많으시네요!"
○  "같은 판단을 세 프로젝트에서 되풀이하셨더라고요. 그래서 한 문서로 묶었습니다."
```

**02 를 막 끝낸 사람이다.** 02 에서 이미 답한 것을 다시 묻지 않는다.
컨셉 · 읽을 사람 · 말투 · 표기는 `컨셉.md` 에 있다. **읽고 쓴다.**

---

## 진행 표시

클릭 질문마다 앞에 `[3/5]` 를 붙인다. **그게 전부다.**
"조금만 더 힘내세요" 같은 말은 넣지 않는다. **실제 개수를 쓴다.**

### 클릭 질문은 반드시 하나씩

**질문을 두 개 이상 한 창에 넣지 않는다.** 탭이 생기고, 탭을 다 채워야 제출된다.

| | |
|---|---|
| 1 | 질문 하나면 탭이 없고 고르는 즉시 넘어간다 |
| 2 | 보기는 2~4개, 각각 한 줄 설명 |
| 3 | **빠져나갈 보기를 하나 둔다.** "잘 모르겠다" · "그대로 두자" |
| 4 | 후보 비교가 필요하면 preview 를 쓴다 |

### 서술로 받는 것

셋뿐이다. **클릭 개수에 안 센다.**

| | |
|---|---|
| 1 | "이건 아닌데" (분류 표 뒤) |
| 2 | 허브 이름 고치기 (허브 표 뒤) |
| 3 | "고칠 데" (`plan.md` 뒤) |

**클릭 다섯 개가 전부 「고르기」이지 「하기」가 아니다.** 사람은 판단만 하고 손일은 안 한다.

---

## STEP 1 · 첫 메시지

시작하자마자 여섯 가지를 말한다.
에셋엔진이 뭔지, 이 단계가 그중 어디인지, 뭘 하는 건지, **여기서 안 하는 게 뭔지**,
어떻게 가는지, **미리 알아둘 게 뭔지**.

**아래를 마크다운 그대로 낸다.** 코드블록으로 감싸지 않는다. 감싸면 표가 안 먹고 뭉쳐서 안 읽힌다.
선이나 네모로 모양내지 않는다. 띄어쓰기로 만든 정렬은 화면에서 다 무너진다.

**낸 뒤 멈춘다.** 곧바로 질문 창을 띄우지 않는다. 창이 뜨면 안내문이 가려진다.

내는 내용은 이것이다.

---

## 03 STRUCTURE · 구조 잡기

### 에셋엔진이 뭐냐면

한 일을 **꺼내고, 성과의 언어로 옮기고, 하나의 구조로 이어**
검색과 AI 답변에서 발견되게 만드는 워크플로우입니다.

단계마다 스킬 하나씩이고, 끝나면 다음 스킬을 알려드립니다.
**한 번에 하나만 아시면 됩니다.**

| | |
|---|---|
| 01 재료화 | 흩어진 자료를 한 형식으로 모았습니다 |
| 02 컨셉 잡기 | 반복해오신 일을 찾고 컨셉 한 줄을 정했습니다 |
| **03 구조 잡기** | **지금 여기** |
| 04 화면설계 | 사이트를 어떻게 만들지 그립니다 |
| 05 사이트 짓기 | 실제로 짓고 잘 됐는지 검사합니다 |
| 07 검색·AI 점검 | 사이트가 선 뒤 재고 고칩니다. **06 보다 먼저 합니다** |
| 06 질문 짓고 글 쓰기 | 07 이 만든 규칙으로, 잴 질문을 짓고 그 질문에 답하는 글을 냅니다 |
| 08 글 한 편 재기 | 낸 글이 AI 답변에 걸리나 재고, 다시 06 으로 돌아갑니다 |

### 이 단계가 뭐냐면

자료는 **쓰신 순서**로 있습니다. 회고일지, 수행일지, 이력서, 보고서.
그런데 **남이 궁금해하는 순서는 그게 아닙니다.**

> "AI 툴 쓸지 말지 뭘 보고 정해요?"

이 하나에 답하려면 자료 **세 곳**을 뒤져야 합니다. 흩어져 있어서 아무도 못 찾습니다.
본인도요.

**여기서는 그 셋을 한자리에 모읍니다.** 자료를 옮기는 게 아니라,
자료를 가리키면서 **그 일의 이름을 붙여 놓는 것**입니다.

### 그게 어디에 쓰이냐면

한 번 만들면 뒤 단계가 그대로 받아 씁니다.

| | 여기서 만든 문서 한 장이 |
|---|---|
| 05 사이트 | 페이지 한 개가 됩니다. 주소 하나 |
| 07 글쓰기 | 글 한 편의 뼈대가 됩니다. 근거가 이미 붙어 있어요 |
| 검색·AI | 「이 사람은 뭘 할 줄 아나」에 꺼내 쓰는 조각이 됩니다 |

02 에서 **「지금 AI 는 나를 이렇게 말한다」**를 재두셨죠.
지금은 대체로 안 나옵니다. **여기서 만드는 문서 한 장이 나중에 그 자리 한 줄이 됩니다.**

### 여기서 안 하는 게 있습니다

| | |
|---|---|
| **자료를 새로 받지 않습니다** | 01 에서 이미 다 모았습니다 |
| **화면을 그리지 않습니다** | 04 에서 합니다 |
| **글을 쓰지 않습니다** | 07 에서 씁니다. 여기서는 **글 목록 초안**까지만 |
| **사이트를 짓지 않습니다** | 06 에서 합니다 |

### 어떻게 가냐면

질문 다섯 개면 끝납니다. 그사이에 제가 자료를 가르고 문서를 쓰는 시간이 좀 걸립니다.

| | | |
|---|---|---|
| 1 | 02 에서 정한 컨셉을 읽습니다 | 기다리시면 됩니다 |
| 2 | 자료 묶음이 누가 만든 건지 확인 | 질문 1개 |
| 3 | **제가 다 갈라 놓고 표로 보여드림** | ← 보시고 「이건 아닌데」만 |
| 4 | 직접 만드신 화면을 열어볼지 확인 | 질문 1개 |
| 5 | **주제를 어떻게 묶을지 후보를 드림** | 질문 1개 |
| 6 | 문서를 씁니다 | 기다리시면 됩니다 |
| 7 | **질문 · 글 목록 · 주소 규칙을 보여드림** | 질문 1개 |

중간에 멈추셔도 되고, 모르는 건 안 정하고 넘어가셔도 됩니다.

### 끝나면 뭘 받냐면

**주제 문서 여러 장 + 정리 문서 3장**

| | |
|---|---|
| `wiki/` 폴더 | 주제마다 문서 한 장. **06 이 이걸로 사이트 글을 만듭니다** |
| `구조.md` | 주제를 어떻게 나눴나 · 답할 질문 · **글 목록 초안** · 주소 규칙 |
| `분류.md` | 뭘 쓰고 뭘 안 썼나. **안 쓴 것은 이유가 다 적혀 있습니다** |
| `plan.md` | 지금 어디까지 정해졌고 다음에 뭘 하나 |

### 미리 알려드릴 것

| | |
|---|---|
| **문서** | 자료를 전부 문서로 만들지 않습니다. **컨셉에 붙는 것만 엮습니다.** 안 엮은 것도 목록에는 전부 남아서 나중에 언제든 꺼냅니다 |
| **크기** | 몇 장을 만들겠다고 미리 약속하지 않습니다. **다 만들고 나서 몇 장인지 알려드립니다** |
| **자료** | **하나도 안 지웁니다.** 이번에 안 쓴 것도 그대로 있습니다 |
| **남의 자료** | 강의 교재처럼 남이 만든 것은 문서로 안 엮습니다. **다만 같은 폴더에 본인이 쓰신 회고나 일지가 섞여 있으면 그건 살립니다** |
| **이미지** | 직접 만드신 화면이 섞여 있습니다. **열어서 안에 뭐가 있는지 적을지 여쭤봅니다** |
| **개인정보** | 보이면 문서에서 뺍니다. **값은 화면에도 기록에도 안 남깁니다** |
| **질문** | 몇 개를 만들겠다고 미리 약속하지 않습니다. **답할 자료가 있는 것만 만듭니다** |

---

**지금 다 이해 안 되셔도 됩니다.** 끝까지만 오시면 문서가 남고,
그 순서대로 하나씩 하시면 됩니다.

다 읽으셨으면 **아무 말이나 보내주세요.** 바로 시작할게요.
궁금한 게 있으면 먼저 물어보셔도 됩니다.

---

**이 안내는 한 번만 낸다.** 사용자가 답하면 바로 STEP 2 로 간다.
넘어갈 때 안내를 다시 요약하지 않는다. 중간에도 다시 요약하지 않는다.

---

## STEP 2 · 02 산출물 읽기

**화면에 한 줄만 낸다.** 읽은 내용을 보고하지 않는다.

### 설정을 조용히 읽는다

```bash
cat ~/.claude/iny-config.json
```

`kb_path` 를 꺼내 작업 폴더로 쓴다. `~` 는 실제 홈 경로로 바꾼다.
파일이 없으면 **재료가 어디 있는지 묻는다.** 짐작해서 뒤지지 않는다.

### 02 산출물을 읽는다

```
{작업폴더}/outputs/02-identify/plan.md
{작업폴더}/outputs/02-identify/컨셉.md
{작업폴더}/outputs/02-identify/근거.md
```

**셋 다 읽는다.** `plan.md` 의 「다음 단계로 넘길 것」에 03 줄이 있으면 거기부터 본다.

### 없으면 여기서 멈춘다

**컨셉 한 줄이 없으면 03 이 아예 못 선다.** 3분류의 기준이 그것이기 때문이다.

```
02 에서 만든 컨셉 문서가 안 보입니다.

03 은 02 가 정한 컨셉을 기준으로 자료를 가르는 단계라
02 를 먼저 하셔야 합니다.

Claude Code 에 "02 시작" 이라고 치시면 됩니다.

안 뜨면 아직 안 깔린 것입니다. 둘 중 하나로 하시면 됩니다.
  · 스타터팩을 받으셨으면 → "skills 폴더의 iny-identify 를 내 스킬 폴더로 복사해 줘"
  · 아니면 → https://github.com/iny101400-young/iny-identify
```

`컨셉.md` 는 있는데 `근거.md` 가 없으면 **멈추지 않고 간다.** 근거는 03 이 재료에서 다시 캘 수 있다.
다만 「근거 문서가 없어서 재명명 표를 못 읽었습니다」를 `plan.md` 에 적는다.

### 무엇을 어디에 쓰는지

| 02 가 준 것 | 03 이 쓰는 자리 |
|---|---|
| **컨셉 한 줄** | **3분류의 기준.** 「글감인가 근거인가 안 씀인가」를 이걸로 가른다 |
| 읽을 사람 (연락 올 사람 · 읽으러 올 사람) | **허브 바깥 층** |
| 재명명 표 | **위키 문서 제목** 그리고 **허브 안쪽 층** |
| 반복한 일 갈래 | 클러스터 후보 |
| 재료에서 나온 질문 | 질문 ① 씨앗 |
| **떨어진 두 안** | **인접 클러스터 씨앗.** 아래 |
| 안 읽은 재료 목록 | 03 이 다시 훑을 자리 |
| 개인정보 절 | ★ **이미 판정한 자리다. 다시 안 묻는다** |

### 떨어진 두 안에서 질문을 캔다

02 는 컨셉 세 가지를 내고 하나로 좁힌다. `컨셉.md` 의 **「떨어진 두 안과 이유」** 절에
안 고른 두 안이 이유와 함께 남아 있다.

**그 두 안이 답하려던 질문이 인접 클러스터의 씨앗이다.**
02 가 그 질문을 따로 안 넘겨줄 수 있다. **그러면 03 이 그 절을 읽고 직접 캔다.**
안 고른 안이라고 재료가 사라진 게 아니다. 근거는 `근거.md` 에 그대로 있다.

### 화면에는 이 한 줄만

```
02 에서 정하신 컨셉을 읽었어요.

  「{컨셉 한 줄}」

이걸 기준으로 자료를 가르겠습니다.
```

**컨셉 한 줄을 그대로 되읽어 준다.** 이 뒤 전부가 이 문장 하나에 걸리기 때문이다.

「한 줄」이라고 부르지만 **여러 줄일 수 있다.** 사용자가 직접 쓴 문장인 경우가 많다.
**요약하지 않고 블록째 낸다.** 줄이면 그 사람이 쓴 말이 아니게 된다.

---

## STEP 3 · 훑기

**화면에 아무것도 안 낸다.** 여기서 나온 숫자는 STEP 5 의 표로만 나간다.

```bash
python3 "{이 스킬이 설치된 폴더}/scripts/scan.py" "{작업폴더}" --조용히
```

내는 것 둘.

| | |
|---|---|
| `.kb/구조지도.json` | 묶음별 집계 · 폴더 끝 사람 이름 · 이미지 갈래 · 개인정보 후보 |
| `.kb/분류표.tsv` | 파일 한 줄씩. 규칙으로 걸린 것만 `분류` 칸이 채워져 있다 |

**02 가 낸 `.kb/재료지도.json` 과 `.kb/재료목록.tsv` 는 건드리지 않는다.**
같은 파일을 두 스킬이 서로 덮어쓰면 나중에 돌린 쪽이 이긴다.

스크립트가 하는 판단은 넷뿐이다. 세는 것, 같은 것끼리 묶는 것, 제외 목록을 반영하는 것,
**규칙으로 확실히 걸리는 것만 분류에 채우는 것**. 쓸모는 안 고른다.

### 스크립트가 이미 채워 놓는 것

| 분류 | 규칙 |
|---|---|
| `안씀:중복` | 본문 해시가 같다. 대표 하나만 남는다 |
| `안씀:빈것` | 본문이 100자 미만. **이미지는 여기서 뺀다** |

**이미지는 자동으로 「안 씀」에 안 들어간다.** 본문이 0자인 건
「내용이 없는 것」이 아니라 **「안에 뭐가 있는지 아직 안 적힌 것」**이다.

### 파이썬이 없거나 스크립트가 안 돌면

직접 훑는다. 다만 **아래 셋을 반드시 지킨다.**

**하나. 첫 `---` 블록만 걷어낸다.**

01 은 원본 머리말을 지우지 않고 그 위에 자기 머리말을 얹었다. 두 겹인 파일이 많다.
둘째 블록까지 지우면 **본문이 통째로 날아간다.** 실제로 그렇게 헛돌아
멀쩡한 파일 수백 개가 "본문 없음" 으로 나온 적이 있다.

**둘. 같은 본문끼리 묶은 뒤에 센다.** 안 묶고 세면 반복 횟수가 틀린다.

**셋. 한글 파일명을 NFC 로 맞춘다 (맥).**

```python
import unicodedata
p = unicodedata.normalize('NFC', p)
```

01 에서 이걸 안 해서 필터가 통째로 헛돌았다. **03 도 `grep` 을 쓰니 똑같이 걸린다.**
`grep -c "글자"` 가 0인데 `ls` 로는 그 경로가 보이면 이 문제다.

### 제외 목록을 반드시 반영한다

```bash
cat {작업폴더}/제외목록.md
```

**01 이 이미 만들어뒀고 첫 줄이 03 을 지목해 놨다.**
스크립트가 이걸 이미 한다. 없으면 만들지 말고 넘어간다.

---

## STEP 4 · 이 묶음들은 누가 만든 건가

**여기가 03 에서 제일 잘 틀리는 자리다.**

학원 · 부트캠프 · 사내 교육 자료는 **강사 자료와 본인 제출물이 한 폴더에 있다.**
폴더 이름으로 가르면 둘이 같이 버려진다.

> **폴더 이름으로 가르지 않는다. 폴더 끝의 사람 이름으로 가른다.**

`.kb/구조지도.json` 의 `사람이름` 과 `이름붙은묶음` 이 후보를 이미 뽑아놨다.
**한 번에 묻는다.** 나중에 하나씩 되묻지 않는다.

### 화면에는 이렇게 나간다

```
자료 폴더 이름 끝에 사람 이름이 붙어 있는 게 있어요.
어느 게 본인 것인지만 알려주시면 나머지는 제가 가릅니다.

  {이름}   {N}개   {글자수}자   예) {폴더 이름 하나}
  {이름}   {N}개   {글자수}자   예) {폴더 이름 하나}
```

**[1/5] 이 중에 본인이 만드신 건 어느 건가요?** (복수 선택)

보기는 `구조지도.json` 이 뽑은 **실제 이름**으로 채운다. 보기마다 개수와 예시 폴더를 한 줄로 붙인다.
마지막에 **"잘 모르겠다"** 를 둔다. 고르면 그 묶음 전체를 `보류` 로 두고 위키에 안 엮는다.

### 고르고 나면

| 고른 이름 | 나머지 이름 |
|---|---|
| 그 폴더 것은 다른 재료와 똑같이 컨셉 기준으로 가른다 | `안씀:남의것` |

**본인 것이 아니어도 재료에서 빼지 않는다.** 뭘 배웠나의 흔적으로는 값이 있다.

### ⚑ 여기서 끝내면 안 된다 · 남의 폴더 안에 본인이 쓴 것이 있다

**폴더 끝 이름만 보고 통째로 빼면 본인이 쓴 것이 같이 날아간다.**

학원·부트캠프 폴더는 **그날 강사가 나눠준 교재와 그날 본인이 쓴 회고가 한자리에 있다.**
폴더는 강사 이름으로 끝나는데 그 안의 회고는 본인 것이다.

```
3주차_실습_이강사/
    3주차_강의교재.pdf        ← 강사가 만든 것
    4Ls-회고-3주차.md         ← ★ 본인이 쓴 것. 폴더 이름만 보면 같이 빠진다
    3주차_수행일지.md          ← ★ 본인이 쓴 것
```

**실측에서 이게 41개였고, 그중 다섯이 02 가 근거로 쓴 것이었다.**
컨셉이 「헤맨 기록이 자산」인 사람인데 그 헤맨 기록이 전부 강사 폴더 안에 있었다.

### 되찾는 순서 셋. 위에서부터 세다

| 순서 | 무엇 | 어떻게 |
|---|---|---|
| **1** | **02 가 근거로 쓴 파일** | `근거.md` 의 링크를 전부 모은다. **여기 있으면 무조건 살린다.** 제일 확실한 신호다 |
| **2** | 파일 이름이 본인 것으로 보이는 것 | `구조지도.json` 의 `되찾을후보` 가 이미 뽑아놨다. 회고 · 수행일지 · 복습노트 · 과제 · TIL 같은 말 |
| **3** | 본문을 열어 확인 | 1인칭으로 자기가 한 일을 쓴 글인지. **강사 말투면 안 살린다** |

살릴 것은 `분류규칙.tsv` 에 **파일 하나씩 `길` 규칙으로** 적는다.
`길` 이 `이름끝` 을 이기므로 그 파일만 살아난다.

```
이름끝	{강사 이름}	안씀:남의것
길	raw/notes/4ls-회고-3주차-a1b2c3.md	글감
길	raw/notes/3주차_수행일지-d4e5f6.md	글감
```

**사람에게 파일을 하나씩 묻지 않는다.** 내가 다 살려놓고 STEP 5 표에서 같이 짚게 한다.

### 이름이 하나도 안 붙어 있으면

이 질문을 건너뛴다. **클릭 개수를 `[1/4]` 로 줄이지 않는다.**
대신 `구조지도.json` 의 `묶음` 목록에서 남의 것으로 보이는 묶음을 후보로 내고 같은 질문을 한다.
후보도 없으면 **이 클릭을 통째로 건너뛰고 그 사실을 `분류.md` 에 한 줄 적는다.**

---

## STEP 5 · 3분류를 다 해놓고 보여준다

**여기서 사람이 파일을 하나씩 판정하게 하지 않는다.**

> **백지에 묻지 않는다. 먼저 채워놓고 고치게 한다.**

| | 누가 | 무엇 |
|---|---|---|
| 가른다 | **내가** | 컨셉 한 줄을 기준으로 남은 것을 전부 갈라 놓는다. 안 씀에는 이유를 적는다 |
| 짚는다 | **사람** | 묶음 단위 표를 보고 **「이건 아닌데」만** 말한다 |

### 셋으로 가른다

| | 무엇 | 이 뒤에 어떻게 되나 |
|---|---|---|
| **글감** | 컨셉에 붙고 **이 사람이 한 일**이 적혀 있다 | 위키로 엮는다 |
| **근거** | 컨셉에 붙지만 그 자체로 주제가 안 된다 | 위키에 안 엮되 **07 이 인용할 수 있게 남긴다** |
| **안 씀** | 이번에 안 쓴다 | 목록에만 남는다. 이유를 적는다 |

**「근거」 칸을 없애지 않는다.** 「안 씀」과 「지금 안 쓰지만 근거로는 남긴다」는 다르다.
합치면 07 이 인용할 재료를 다시 찾아야 한다.

### 가르는 기준

`컨셉.md` 를 그대로 쓴다. **내가 새로 컨셉을 만들지 않는다.**

| 이렇게 물어본다 | 그러면 |
|---|---|
| 이 재료가 컨셉 한 줄과 이어지나 | 안 이어지면 `안씀:컨셉과안이어짐` |
| 이 사람이 **한 일**이 적혀 있나, 아니면 **배운 것**이 적혀 있나 | 한 일이면 글감, 배운 것이면 근거 |
| 「읽을 사람」이 궁금해할 만한가 | 궁금해할 만하면 글감 쪽으로 |
| 그 자체로 질문 하나에 답하나 | 답하면 글감, 조각이면 근거 |

### ⚑ 컨셉 안에 근거가 없다고 표시된 부분은 기준에서 뺀다

02 는 컨셉 문장 중 **재료에 뒷받침이 없는 자리**를 스스로 표시해 둔다.
`컨셉.md` 본문이나 `근거.md` 의 「비어 있는 칸」에 그 자리가 적혀 있다.

> "「{어느 구절}」 한 줄은 재료에 뒷받침이 없다."

**그 구절로 재료를 고르지 않는다.** 근거가 없는 말을 기준으로 삼으면
그 말에 맞는 재료를 찾게 되고, 없으면 지어내게 된다. **02 가 비워둔 자리를 03 이 메우지 않는다.**

`분류.md` 에 **「이 구절은 기준에서 뺐습니다」를 한 줄 적는다.** 조용히 빼지 않는다.

**02 가 이미 읽은 것을 기본값으로 깐다.** `근거.md` 의 근거 목록에 링크된 재료는
02 가 골라 읽고 근거로 쓴 것이다. **그것부터 글감·근거 쪽에 놓고 차이만 다시 본다.**
02 를 막 끝낸 사람에게 같은 판단을 처음부터 다시 시키지 않는다.

### 규칙 파일을 쓴다

파일 수백 개를 하나씩 적지 않는다. **묶음 단위로 규칙을 쓰고 예외만 파일로 적는다.**

`{작업폴더}/.kb/분류규칙.tsv` 에 탭으로 나눈 네 칸을 쓴다.

```
# 갈래	값	분류	위키
이름끝	{남의 이름}	안씀:남의것
묶음	{묶음 이름}	글감
묶음2	{묶음/하위폴더}	글감
길	raw/notes/xxx.md	근거
```

| 갈래 | 무엇 |
|---|---|
| `길` | 파일 하나. **제일 세다** |
| `이름끝` | 폴더 끝 사람 이름 |
| `묶음2` | 묶음 아래 한 겹 |
| `묶음` | 맨 위 묶음 |

좁은 것이 넓은 것을 이긴다. 스크립트가 이미 채운 `안씀:중복` · `안씀:빈것` 은 규칙이 못 덮는다.

그리고 다시 돌린다.

```bash
python3 "{이 스킬이 설치된 폴더}/scripts/scan.py" "{작업폴더}" --분류 "{작업폴더}/.kb/분류규칙.tsv" --조용히
```

### 화면에는 이렇게 나간다

**묶음 단위로 낸다. 파일 목록을 쏟지 않는다.**

```
자료 {N}개를 「{컨셉 한 줄}」 기준으로 갈라 놨어요.

  위키로 엮을 것       {N}개
    {묶음 이름} {N}개        {한 줄 성격}
    {묶음 이름} {N}개        {한 줄 성격}

  근거로만 남길 것     {N}개
    {묶음 이름} {N}개        {한 줄 성격}

  이번에 안 쓸 것      {N}개
    {묶음 이름} {N}개        {왜}
    똑같은 복사본 {N}개       대표 하나만 씁니다
    내용이 거의 없는 것 {N}개
    {남의 이름} 폴더 {N}개    {이름}님이 만드신 자료예요

  ★ {남의 이름}님 폴더 안에 있지만 직접 쓰신 것 같아서 살린 것    {N}개
    {파일 이름}
    {파일 이름}
    강의 폴더 안에 회고랑 수행일지가 같이 있어서요.
    아닌 게 있으면 말씀해 주세요.

자료는 하나도 안 지웠습니다. 안 쓴 것도 그대로 있고 나중에 언제든 꺼냅니다.
```

**★ 살린 것을 반드시 파일 이름까지 보여준다.** 여기가 이 스킬에서 제일 잘 틀리는 자리이고,
틀리면 그 사람의 제일 값어치 있는 자료가 조용히 빠진다. **조용히 살리지도 않는다.**

**「안 쓸 것」을 반드시 같이 낸다.** 조용히 빼지 않는다.
**개인정보 후보 개수를 여기서 말하지 않는다.** STEP 8 에서 문서를 쓰기 직전에 다룬다.

**[2/5] 이대로 갈까요?** (단일 선택)

| 보기 | 설명 |
|---|---|
| `이대로 가자` | `가른 대로 진행합니다` |
| `빼거나 옮길 게 있다` | `어느 묶음인지 말씀해 주시면 고칩니다` |
| `잘 모르겠다` | `이대로 가고 나중에 언제든 고칠 수 있습니다` |

"빼거나 옮길 게 있다" 를 고르면 **서술로 한 줄 받는다.** 클릭으로 못 받는다.
받아서 `분류규칙.tsv` 를 고치고 스크립트를 다시 돌린 뒤 **바뀐 자리만** 한 줄로 말한다.
표를 통째로 다시 내지 않는다.

---

## STEP 6 · 직접 만드신 화면

**형식이 이미지라고 자동으로 빼지 않는다.** 그 안에 이 사람이 만든 화면이 섞여 있다.
글로만 있는 「무엇을 설계했다」에 **그림 근거를 붙이는 유일한 재료**다.

`구조지도.json` 의 `이미지` 칸이 갈래를 이미 뽑아놨다.

| | 어떻게 가르나 |
|---|---|
| 본인이 만든 화면 | STEP 4 에서 고른 이름이 붙은 폴더 · 본인 프로젝트 이름이 든 파일명 |
| 봐야 아는 것 | 그 밖. **조용히 빼지 않고 목록에 「아직 안 봤다」로 남긴다** |
| 남의 것 | STEP 4 에서 남의 이름이 붙은 폴더 |

### 원본이 살아 있는지 먼저 본다

`구조지도.json` 의 `이미지.원본살아있음` 이 몇 장인지 말해준다.
**원본이 없으면 못 연다.** 못 여는 장수를 화면에 같이 말한다.

### 화면에는 이렇게 나간다

```
이미지 {N}장 중에 {N}장이 직접 만드신 화면으로 보여요.

  {폴더 이름}      {N}장
  {폴더 이름}      {N}장

지금은 안에 뭐가 있는지 자료에 안 적혀 있어서 근거로 못 씁니다.
제가 열어서 뭐가 보이는지 적어두면 「{재명명된 일 하나}」에 그림 근거가 붙습니다.

제가 엽니다. 따로 하실 것 없습니다.
```

**[3/5] 이 {N}장을 열어서 안에 뭐가 있는지 적어둘까요?** (단일 선택)

| 보기 | 설명 |
|---|---|
| `네, 열어주세요` | `{N}장을 다 엽니다. 조금 걸립니다` |
| `글감으로 갈린 것만` | `{N}장만 엽니다. 나머지는 목록에만 남습니다` |
| `아니요, 지금은 안 열래요` | `목록에는 「아직 안 봤다」로 남습니다. 나중에 언제든 할 수 있어요` |

**개수를 내가 정하지 않는다.** 세 보기의 `{N}` 은 실제로 센 값을 쓴다.
**"나중에" 라고만 적고 넘어가지 않는다.** 안 열면 뭐가 없어지는지 위에 이미 말했다.

---

## STEP 7 · 허브 두 층

**주제를 어떻게 묶을지 정하는 자리다.** 여기서 정한 것이 06 의 주소와 07 의 글 목록이 된다.

### 두 층으로 겹친다

```
바깥 층   읽으러 올 사람 기준      찾아오는 길. 검색어가 여기 붙는다
  └ 안쪽 층   재명명된 역량 기준     뭘 할 줄 아는 사람인지. 채용하는 사람이 보는 언어
```

| 층 | 어디서 뽑나 | 왜 |
|---|---|---|
| 바깥 (허브) | `컨셉.md` 의 **「읽을 사람」** 절 | 검색어가 이미 거기 적혀 있다. 05 주소 규칙과 07 글 목록이 이걸 쓴다 |
| 안쪽 (클러스터) | `근거.md` 의 **재명명 표** 와 **반복한 일 갈래** | 위키 문서 제목이 여기서 나온다 |

**개수를 미리 안 박는다.** 재료가 붙는 만큼 정해진다.

### 백지에 묻지 않는다

**후보를 다 채워서 낸다.** 그리고 고치게 한다.

```
주제를 두 층으로 묶어봤어요.
큰 갈래는 「누가 찾아오나」, 그 안의 작은 갈래는 「뭘 할 줄 아나」예요.

  {큰 갈래 이름}
      {작은 갈래 이름}      자료 {N}개
      {작은 갈래 이름}      자료 {N}개

  {큰 갈래 이름}
      {작은 갈래 이름}      자료 {N}개

  어디에도 안 붙은 것   {N}개
      {왜 안 붙었나 한 줄}

이건 글을 나누는 칸이지 사이트 메뉴가 아니에요.
사이트를 어떤 메뉴로 짤지는 04 에서 직접 고르시게 됩니다.
```

**「어디에도 안 붙은 것」을 반드시 같이 낸다.** 이게 나중에 고아가 될 자리다.
붙일 데가 없으면 그 재료는 `안씀:컨셉과안이어짐` 으로 내린다. **억지로 안 붙인다.**

### ★ 한 갈래에 몰리면 그 자리에서 말한다

**한 갈래에 자료가 몰리는 일이 흔하다.** 프로젝트 하나에서 나온 자료가 많아서다.
갈래가 열 개인데 자료의 절반이 한 칸에 있으면 **갈래를 나눈 뜻이 없다.**

| 언제 | 무엇 |
|---|---|
| 한 갈래가 **다른 갈래들보다 몇 배로 크면** | 화면에 그 사실을 한 줄 말한다. 숨기지 않는다 |
| 왜 몰렸는지 본다 | 대개 **프로젝트 하나에서 나온 것**이다. 주제가 하나라서가 아니다 |
| STEP 8 에서 | **문서를 쓸 때 여러 장으로 쪼갠다.** 갈래를 쪼개는 게 아니다 |
| 반대로 **너무 작은 갈래**가 있으면 | 그것도 말한다. **02 가 핵심이라 한 자리인데 재료가 얇으면 07 에서 걸린다** |

```
{작은 갈래 이름} 에 {N}개가 몰렸어요. {무엇} 하나에서 나온 자료라서요.
문서를 쓸 때 여러 장으로 쪼개겠습니다.

{작은 갈래 이름} 은 {N}개뿐이에요. 02 가 핵심 자산이라고 한 자리인데 자료가 얇습니다.
```

**갈래를 다시 짜지 않는다.** 갈래는 사람이 고른 것이고, 몰린 건 자료 사정이다.
**쪼개는 건 문서 쪽이다.**

**[4/5] 이렇게 묶을까요?** (단일 선택)

| 보기 | 설명 |
|---|---|
| `이대로 가자` | `이 묶음으로 문서를 씁니다` |
| `이름을 고치고 싶다` | `어느 걸 뭐로 바꿀지 말씀해 주세요` |
| `묶는 방식을 바꾸고 싶다` | `다른 축으로 다시 짜 드립니다` |
| `잘 모르겠다` | `이대로 가고 04 에서 다시 볼 수 있습니다` |

"묶는 방식을 바꾸고 싶다" 를 고르면 **다른 축으로 다시 짠다.**
반복한 일 갈래로 가르는 안과 재명명 역량만으로 가르는 안을 나란히 낸다.

**허브 이름은 화면에 나갈 말이다.** 「연락 올 사람」 같은 내부 용어를 그대로 쓰지 않는다.
읽을 사람이 자기를 알아볼 말로 짓는다. 예를 들어 「같은 일 하는 사람」 · 「이제 시작하는 사람」.

---

## STEP 8 · 이미지 채우기 · 위키 짓기

한 줄만 알리고 **답을 기다리지 않는다.**

```
문서로 엮고 있어요. 조금 걸립니다.
```

**시간을 숫자로 약속하지 않는다.**

### 8-1. 이미지 본문 채우기

STEP 6 에서 "네" 를 받았으면 **여기서 연다.** 나중으로 미루지 않는다.

#### ★ 열기 전에 한 번 더 거른다

**받은 개수를 그대로 열지 않는다.** 「다 열어주세요」를 받아도
그중 절반 가까이가 **열 값이 없는 것**일 수 있다. 열고 나서 알면 그만큼이 헛일이다.

| 거를 것 | 왜 |
|---|---|
| **중복** | 본문 해시가 같다. 대표 하나만 연다 |
| **아이콘 · 썸네일 · 로고** | 파일 이름과 크기로 걸린다. 화면이 아니다 |
| **남의 폴더에서 온 것** | STEP 4 에서 남의 이름이 붙은 폴더. 열어도 못 쓴다 |
| **깨진 파일 · 원본이 없는 것** | 열 수가 없다 |

**거르고 나서 화면에 한 줄 말한다.** 조용히 줄이지 않는다.

```
{N}장 중 {N}장은 열 값이 없어서 뺐어요.
   중복 {N}장 · 아이콘 {N}장 · {남의 이름}님 폴더 {N}장 · 원본이 없는 것 {N}장
   {N}장을 엽니다.
```

**약속한 개수와 실제 개수가 다르면 그 차이를 반드시 말한다.**
「다 열어주세요」를 받고 절반만 열었다면 **왜 나머지를 뺐는지 말해야 약속을 지킨 것이다.**
`분류.md` 에도 같은 표를 적는다.

#### 여는 순서

| | |
|---|---|
| 1 | 재료 md 의 머리말 `source` 가 **원본 이미지의 절대경로**다. 그걸 연다 |
| 2 | 한 번에 여러 장씩 연다. **한 장 열고 한 장 쓰기를 되풀이하지 않는다** |
| 3 | 본 것을 **재료 md 본문에 쓴다.** 내 파일에 따로 적지 않는다 |
| 4 | 머리말의 `described: no` 를 지운다. **덮어쓰기가 아니라 이어받기다.** 01 이 그 자리를 만들어뒀다 |
| 5 | 묶음 하나가 끝날 때마다 **어디까지 했는지 남긴다.** 중간에 끊겨도 이어서 한다 |

**뭘 적나.** 화면에 뭐가 보이는지, 무엇을 하는 화면인지, 눈에 띄는 수치나 이름.
**꾸며 쓰지 않는다.** 안 보이는 걸 있다고 적지 않는다.

> ⚠️ **화면 캡처에 연락처나 이메일이 보일 수 있다.**
> 그 값을 서술에 옮겨 적지 않는다. 「연락처가 보인다」까지만 적는다.

원본이 없어서 못 연 것은 **개수를 세어 `분류.md` 에 적는다.** 조용히 넘기지 않는다.

### 8-2. 위키 문서 쓰기

[`references/위키규칙.md`](references/위키규칙.md) 를 연다. 양식과 링크 규칙이 거기 있다.

**한 장이 질문 하나에 온전히 답하는 크기다.**

| | |
|---|---|
| 재료가 둘 이상 붙고 질문 하나에 답한다 | 맞는 크기다 |
| 재료가 하나뿐이고 얇다 | 위 문서 안으로 넣는다. 한 장으로 안 세운다 |
| 질문이 여럿 섞여 있다 | 쪼갠다 |

**제목은 재명명된 이름으로 짓는다.** `근거.md` 의 재명명 표 오른쪽 칸이다.
본인이 쓴 말이 아니라 **가려는 자리에서 쓰는 말**이다.

머리말 세 줄(`허브` · `클러스터` · `질문`)을 비우지 않는다.
`질문` 칸이 문서를 쓸 때 채워지므로 **답할 재료가 없는 질문이 애초에 안 생긴다.**

### 8-3. 쓰기 전에 개인정보를 막는다

**03 은 02 보다 한 칸 세다.** 02 는 근거를 뽑을 뿐이지만 03 은 문서를 새로 쓰고
그게 06 에서 사이트가 되고 07 이 발행한다. **위키에 한 번 들어간 연락처는 그대로 공개된다.**

| 순서 | |
|---|---|
| 1 | `근거.md` 의 「개인정보」 절을 본다. **02 가 이미 판정한 자리는 다시 안 묻는다** |
| 2 | 02 가 「실습용 가상 데이터」로 판정한 것이면 그대로 쓴다 |
| 3 | 02 가 안 본 자리인데 `구조지도.json` 의 `개인정보.후보` 에 있으면 **그 재료를 열어 확인한다** |
| 4 | 실제 값이면 ★ **아래대로 가린다.** 재료를 빼지 않는다 |
| 5 | 값은 화면에도 문서에도 기록에도 안 옮긴다. **경로와 종류만** |

### ★ 빼는 게 아니라 가린다

**1회차에 이 자리가 틀렸다는 게 드러났다.** 스킬은 「그 재료를 위키에서 뺀다」였는데
**위키에서만 빼면 값이 재료에 그대로 남는다.** 재료는 07 도 열고 06 도 읽는다.
**값이 살아 있으면 언제든 다시 새어 나간다.**

02 가 이미 더 센 방식을 쓰고 있었다. **그걸 따른다.**

| 순서 | 무엇 | |
|---|---|---|
| 1 | 재료 md **본문의 값을 `[가림]` 으로 바꾼다** | 문장은 남기고 값만 지운다 |
| 2 | 머리말에 `sensitive: {종류}` · `masked_at: {날짜}` 를 붙인다 | 어떤 종류였는지만. **값은 안 적는다** |
| 3 | `.kb/manifest.json` 에도 같은 줄을 넣는다 | 다음 단계가 읽는다 |
| 4 | **원본 파일은 절대 안 건드린다** | PDF·문서 원본은 그대로 둔다 |
| 5 | **재료는 살린다.** 위키에도 그대로 쓴다 | 값이 가려졌으니 쓸 수 있다 |

**「안 씀 · 개인정보」로 내리는 건 가릴 수 없을 때만이다.**
문장 전체가 개인정보라 값을 빼면 남는 게 없는 경우다. 그때는 이유를 `분류.md` 에 적는다.

**02 가 47건을 「가상 데이터라 안 가린다」고 판정한 적이 있다.**
가리면 그 실습 기록 자체가 안 읽힌다는 게 이유였다. **그 판단을 뒤집지 않는다.**

> **⚠ 이미지에서 본 것도 같다.** 화면 캡처에 남의 얼굴이나 연락처가 보이면
> **서술에 안 적고** 그 이미지를 `안씀:개인정보` 로 내린다. 이미지는 값을 가릴 수가 없다.

### 8-4. 목차를 쓴다

`{작업폴더}/wiki/index.md` 를 쓴다. 01 이 만들어둔 자리다.

```markdown
# 위키 목차

{날짜} · 문서 {N}장 · 자료 {N}개에서

## {허브 이름}

### {클러스터 이름}

[{문서 제목}](concepts/{파일이름}.md)
{한 줄 요약}

## 여기 없는 것

`sources/` 는 비워 뒀습니다. 자료 하나마다 요약 문서를 만드는 자리인데,
그 문서는 원래 자료보다 정보가 적습니다.
「이 주제가 어느 자료에서 나왔나」는 문서 안의 「어디서 나왔나」 표에 있습니다.
```

**한 줄 요약을 반드시 붙인다.** 빠진 연결을 찾을 때 이 줄만 보고 고른다.

---

## STEP 9 · 빠진 연결 찾기 · 검사

화면에 안 낸다. 고치고 나서 STEP 10 에서 한 번에 말한다.

### 9-1. 빠진 연결

[`references/위키규칙.md`](references/위키규칙.md) 4절. **2단으로 돈다.**

| 단 | 무엇 |
|---|---|
| 1단 | `index.md` 의 한 줄 요약만 보고 볼 자리를 고른다 |
| 2단 | 고른 자리마다 관련 문서를 열어 깊게 읽는다 |

**개수를 안 정한다.** 찾는 것 넷 중 **「비어 있는 것」이 07 의 글감 목록이 된다.**
여러 문서가 가리키는데 정작 그 문서가 없는 주제다.

**증거가 약하면 문서를 안 쓰고 「찾았지만 안 씀」으로 기록한다.**

### 9-2. 검사

```bash
python3 "{이 스킬이 설치된 폴더}/scripts/lint.py" "{작업폴더}"
```

짚힌 것을 **내가 고친다.** 사람에게 목록을 던지지 않는다.

| 짚힌 것 | 어떻게 |
|---|---|
| `A` 얇다 | 재료를 더 붙이거나 위 문서로 합친다 |
| `B` 없는 문서 | 만들거나 링크를 뺀다 |
| `C` 깨진 링크 | 경로를 고친다. 한글이면 NFC 를 의심한다 |
| `D` 겹쳐 보임 | 합치고 링크를 다 고친다 |
| `F` 고아 | 허브를 채우거나 목차에 올린다 |
| `G` 근거 없음 | 출처를 붙인다. **못 붙이면 그 문서를 지운다** |
| `H` 빈 재료에서 만듦 | **그 문서를 지운다** |
| `I` 개인정보 | ★ **멈추고 8-3 으로 돌아간다** |

**고치고 다시 돌린다.** 다 지워질 때까지 돈다.
두 번 돌려도 안 지워지는 것은 **`plan.md` 에 왜 못 고쳤는지 적는다.**

---

## STEP 10 · 질문 · 글 목록 · 주소 규칙

### 10-1. 질문을 만든다

**개수를 미리 안 박는다.** 셋에서 나온다.

| | 어디서 | 어떻게 |
|---|---|---|
| ① | **재료에 실제로 있던 질문** | `근거.md` 의 「재료에서 나온 질문」. 제일 값어치가 크다 |
| ② | 떨어진 두 안에 붙어 있던 질문 | `컨셉.md` 의 「떨어진 두 안과 이유」에서 캔다 |
| ③ | **위키 문서의 `질문` 머리말** | 문서가 서고 거기서 나온 것이라 답할 재료가 반드시 있다 |
| ✕ | 검색량 데이터 · 남의 사이트 FAQ | **안 쓴다.** 재료가 답을 못 가진 질문이 섞인다 |

**③이 핵심 장치다.** 질문을 먼저 만들고 재료를 찾는 게 아니라 문서가 먼저 서고 거기서 질문이 나온다.
그래서 **「고아 질문 0」이 검사가 아니라 설계로 보장된다.**

### 속기록에서 질문을 캘 때

**「남이 나에게 물어본 것」이 얇게 나오는 게 보통이다.** 02 에서 몇 건에 그쳤을 수 있다.
속기록의 화자가 `Speaker A` 처럼 익명이라 누가 누구에게 물었는지 안 갈리기 때문이다.

**사람에게 속기록을 열어보게 하지 않는다. 내가 읽고 추려서 확인만 받는다.**

| | |
|---|---|
| 1 | 속기록을 읽고 **내용으로** 어느 화자가 본인인지 짐작한다. 재명명 표의 일과 회사 이름이 단서다 |
| 2 | **본인이 남에게 물은 것**과 **남이 본인에게 물은 것**을 가른다. 값어치가 다르다 |
| 3 | 남이 물은 쪽만 골라 **문장 그대로** 아래 목록에 올린다 |
| 4 | 클릭 ⑤ 에서 「이건 나한테 온 게 아닌데」를 받는다 |

**남의 유튜브 속기에서 나온 질문은 안 쓴다.** 남의 저작물이다.

### 10-2. 글 목록 초안

클러스터마다 **쓸 수 있는 글**을 뽑는다. **여기서 글을 쓰지 않는다.** 07 이 쓴다.

| 어디서 | 무엇 |
|---|---|
| 위키 문서 | 한 장이 글 한 편의 뼈대다 |
| 9-1 의 「비어 있는 것」 | **여러 곳이 가리키는데 문서가 없는 주제.** 제일 쓸 값이 크다 |
| 재료의 회고·실패기록 | 안 된 것이 제일 값어치 있다 |

**이미 다른 곳에 올린 글이 있으면 표시한다.** 02 가 물었는데 답이 없었을 수 있다.
**여기서 다시 묻는다.** 같은 글이 두 곳에 있으면 08 에서 어느 쪽이 효과 있었는지 못 가른다.

### 10-3. 주소 규칙

**★ 03 은 사이트 구조를 정하지 않는다.** 여기를 넘으면 04 가 할 일을 빼앗는다.

**탭이 몇 개고 뭐가 들어가는지는 03 이 모른다.** 그건 그 사람이 무슨 사이트를 만드느냐에 달렸다.
포트폴리오 사이트인지 브랜드 사이트인지, 만든 것을 앞에 낼지 글을 앞에 낼지.
**04 에서 그 사람이 고른다.**

03 이 아는 건 하나뿐이다.

> **글이 들어갈 자리 하나가 필요하고, 그 안에서 글이 큰 갈래와 작은 갈래로 나뉜다.**

그 자리를 여기서는 `/articles` 라고 부른다. **이름도 04 가 바꿀 수 있다.**

```
{글이 들어갈 자리}
    └ 큰 갈래
        └ 작은 갈래
            └ 글 한 편        ← 03 이 만든 문서 한 장
```

| | 03 이 정하나 |
|---|---|
| 글이 몇 편이고 어떻게 나뉘나 | **정한다.** 이게 03 의 일이다 |
| 글 자리의 이름 (`/articles` · `/blog` · `/log`) | **안 정한다.** 04·05 |
| 탭이 몇 개고 뭐가 들어가나 | **안 정한다.** 04 |
| 갈래를 주소에 넣을지 | **안 정한다.** 후보만 낸다 |

주소 후보는 둘을 낸다.

```
가  /{글 자리}/{글}                   갈래를 주소에 안 넣는다. 짧다
나  /{글 자리}/{큰 갈래}/{글}          갈래가 주소에 보인다. 길다
```

| | |
|---|---|
| 소문자로 고정 | `컨셉.md` 의 표기 네 칸과 같은 이유다. 섞이면 같은 사람으로 안 묶인다 |
| 한글을 그대로 둘지 | **묻지 않는다. 05 BUILD 가 고른다.** 여기서는 이름 후보만 낸다 |
| 날짜를 안 넣는다 | 글이 낡아 보이고 고치면 주소가 죽는다 |

> **03 이 정하는 건 「무슨 글이 몇 편 있고 어떻게 나뉘나」까지다.**
> 그게 어느 탭에 걸리고 주소가 어떻게 생기는지는 04·05 가 정한다.
> **03 이 미리 정해버리면 04 가 화면을 못 바꾼다.**

### 10-4. 화면에는 이렇게 나간다

```
문서 {N}장으로 엮었어요. 자료 {N}개에서 나왔습니다.

■ 어떻게 묶였나
   {큰 갈래}
       {작은 갈래}   문서 {N}장
   {큰 갈래}
       {작은 갈래}   문서 {N}장

■ 이 구조가 답하는 질문
   {질문}                       ← {어디서 나왔나}
   {질문}                       ← {어디서 나왔나}

■ 지금 쓸 수 있는 글
   {글 제목 초안}               근거 {N}개가 이미 붙어 있어요
   {글 제목 초안}               ← 여러 문서가 가리키는데 아직 안 쓰신 것

■ 주소는 이렇게 갑니다
   /{글 자리}/{글}
   글 자리 이름과 갈래를 주소에 넣을지는 04·05 에서 정합니다

■ 안 엮었어요   {N}개
   {묶음} {N}개는 {왜}
   똑같은 복사본 {N}개
   이미지 {N}장은 아직 안 봤어요
   나머지는 컨셉과 안 이어져서 뺐어요. 이유는 분류.md 에 한 줄씩 있어요
   {남의 이름}님 폴더 안이지만 직접 쓰신 것 {N}개는 살렸어요
   자료는 하나도 안 지웠습니다
```

**「안 엮었어요」 칸을 반드시 넣는다.** 02 결과지의 「안 읽었어요」와 같은 자리다.

**[5/5] 이대로 갈까요?** (단일 선택)

| 보기 | 설명 |
|---|---|
| `이대로 가자` | `문서로 쓰겠습니다` |
| `빼거나 고칠 질문이 있다` | `어느 건지 말씀해 주세요` |
| `글 목록을 더 보고 싶다` | `갈래마다 더 뽑아 드립니다` |
| `잘 모르겠다` | `이대로 가고 07 에서 다시 고를 수 있습니다` |

---

## STEP 11 · 문서 쓰기 · 마무리

**여기가 마지막이다.** 아래를 **한 덩어리로** 낸다. 사이에서 멈추지 않는다.

쓰기 전에 한 줄 알리고 **답은 안 기다린다.**

```
정리해서 문서로 쓸게요.
```

쓰는 곳은 `{작업폴더}/outputs/03-structure/` 다.
**이미 내용이 있으면 덮어쓰지 않고 묻는다.**

> 여기 지난번에 만든 게 있어요. 덮어쓸까요, 옆에 새로 만들까요?

### plan.md

```markdown
# {표시 이름} · 지금 어디고 다음에 뭘 하나
{날짜} · 03 STRUCTURE · 자료 {N}개 중 {N}개를 엮음 · 문서 {N}장

## 컨셉 한 줄
{02 에서 정한 것 그대로}

## 정해진 것
| | 정해짐 | 어디에 |
|---|---|---|
| 주제 묶음 (허브 · 클러스터) | ○ | 구조.md |
| 위키 문서 {N}장 | ○ | wiki/ |
| 이 구조가 답하는 질문 {N}개 | ○ | 구조.md |
| 글 목록 초안 {N}개 | ○ | 구조.md |
| 주소 규칙 | ○ | 구조.md |
| 글감 / 근거 / 안 씀 | ○ | 분류.md |

## 안 정해진 것
| 무엇 | 왜 비었나 | 언제 정하면 되나 |
|---|---|---|
| {항목} | {왜} | {언제} |

## 안 엮은 자료
| 묶음 | 개수 | 왜 |
|---|---|---|
| {묶음} | {N} | {왜} |

자료는 하나도 안 지웠습니다. 필요하면 언제든 골라서 더 엮을 수 있습니다.

## 다음에 할 일
1. {사람이 할 일}
2. {사람이 할 일}

## 다음 단계로 넘길 것
| 어디로 | 무엇 |
|---|---|
| 04 DESIGN | 큰 갈래 {N}개 · 작은 갈래 {N}개 · 글 {N}편. **글 자리 안을 어떻게 나눌지의 재료다. 탭 구성은 04 에서 고른다** |
| 04 DESIGN | 큰 갈래 · 작은 갈래 · 글 목록 초안 · 답하는 질문 |
| 05 BUILD | 주소 후보 두 가지 · 이름 후보 · `wiki/` 통째로 |
| 06 PUBLISH | 글 목록 초안 {N}개 · 「비어 있는 것」 {N}개 |
| 07 TECH | 갈래별 검색어 |
| 08 MEASURE | 이 구조가 답하는 질문 {N}개. **같은 질문을 AI 에 던져서 잰다** |

## 04 로 가려면
Claude Code 에 「04 시작」 이라고 치시면 됩니다.
안 뜨면 아직 안 깔린 것입니다. https://github.com/iny101400-young/iny-design
```

**「다음에 할 일」에 이 스킬이 할 일을 적지 않는다.** 거기 적히는 건 사람이 나중에 할 일이다.

### 구조.md

| 담는 것 | |
|---|---|
| 허브와 클러스터 | 두 층. **왜 이렇게 갈랐는지 한 줄씩** |
| 이 구조가 답하는 질문 | 질문마다 **어느 문서가 답하나**를 붙인다 |
| 글 목록 초안 | 클러스터별. **근거가 몇 개 붙어 있는지** 같이 |
| 「비어 있는 것」 | 여러 문서가 가리키는데 아직 문서가 없는 주제. **07 이 여기부터 쓴다** |
| 주소 규칙 | 허브 슬러그 · 문서 슬러그 후보 |
| 찾았지만 안 쓴 것 | 증거가 약해서 문서로 안 만든 것 |

### 분류.md

**파일 1,000줄짜리 목록을 만들지 않는다. 아무도 안 본다.**

| 무엇 | 어디까지 적나 |
|---|---|
| 규칙으로 걸린 것 (중복 · 빈 것 · 남의 것) | **묶음마다 한 줄.** 개수와 글자수 |
| 컨셉과 안 이어져서 뺀 것 | **파일마다 한 줄.** 판단한 자리라 되돌릴 근거가 필요하다 |
| 개인정보로 뺀 것 | **경로와 종류만.** 값은 어디에도 안 옮긴다 |
| 이미지 | 채운 것 · 안 채운 것 · **원본이 없어 못 연 것** |
| 근거로만 남긴 것 | 묶음마다 한 줄. **07 이 여기서 인용한다** |

**개수가 02 산출물과 다르게 나올 수 있다.** 세는 기준이 다르기 때문이다.
02 는 「강의자료」를 회차별 문서로만 셌고, 03 은 **폴더 끝 사람 이름으로 폴더째** 센다.
**틀린 게 아니라 기준이 다른 것이다. 어떻게 셌는지 한 줄 적는다.**

맨 끝에 이 줄을 넣는다.

```
자료는 하나도 안 지웠습니다. 「안 씀」은 이번에 안 쓴다는 뜻입니다.
```

### 다 쓰고 나서 · 여기서 멈추지 않는다

**아래를 이어서 바로 낸다.** `plan.md` 를 보여주고 답을 기다리지 않는다.

먼저 `plan.md` 내용을 채팅창에 그대로 한 번 보여준다.

그다음 답을 못 받은 질문이 있으면 **반드시 말한다.** 조용히 넘기지 않는다.

그리고 한 번 더 확인시킨다.

| | |
|---|---|
| 자료 | 하나도 안 지웠습니다. 안 엮은 것도 그대로 있습니다 |
| 안 엮은 것 | `분류.md` 에 개수와 이유가 적혀 있습니다 |
| 개인정보 | 값은 어디에도 안 남겼습니다. 어떤 종류였는지만 있습니다 |
| 문서 | 주제 문서 {N}장입니다. 06 이 이걸로 사이트 글을 만듭니다 |

마지막으로 **링크와 설치 명령까지** 준다. 링크만 주고 끝내지 않는다.

```
다음은 04 DESIGN 입니다.
방금 정한 구조로 사이트 화면을 그리는 단계입니다.
문서 {N}장이 글 {N}편이 됩니다.
사이트를 어떤 메뉴로 짤지는 거기서 직접 고르시게 됩니다.

Claude Code 에 "04 시작" 이라고 치시면 됩니다.

안 뜨면 아직 안 깔린 것입니다. 둘 중 하나로 하시면 됩니다.
  · 스타터팩을 받으셨으면 → "skills 폴더의 iny-design 을 내 스킬 폴더로 복사해 줘"
  · 아니면 → https://github.com/iny101400-young/iny-design
방금 만든 wiki/ 와 구조.md 를 그대로 읽습니다.
```

**같은 링크를 `plan.md` 에도 적는다.** 화면은 스크롤되면 사라진다.

**그리고 맨 끝에 이 한 줄로 닫는다.**

```
쭉 보시고 고칠 데만 말씀해 주세요.
```

**이 줄 뒤에 아무것도 두지 않는다.** 여기가 끝이다.

---

## 지켜야 할 것

| | |
|---|---|
| 재료 | 지우거나 옮기지 않는다. **이미지 본문을 채우는 것만 예외**이고 01 이 그 자리를 만들어뒀다 |
| 다 엮기 | **약속하지 않는다.** 훑고 · 가르고 · 고른 것만 엮고 · 안 엮은 걸 말한다 |
| 장수 | **미리 안 박는다.** 조건 셋을 걸고 다 만든 뒤에 몇 장인지 말한다 |
| 질문 개수 | **미리 안 박는다.** 답할 재료가 있는 것만 만든다 |
| 안 엮은 것 | 숨기지 않는다. **개수와 이유를 화면과 `분류.md` 에 둘 다 적는다** |
| 폴더 이름 | 폴더 이름으로 주인을 안 가른다. **폴더 끝의 사람 이름으로 가른다** |
| 이미지 | 형식이 이미지라고 자동으로 안 뺀다. **열기로 했으면 그 자리에서 연다.** 다만 ★ **열기 전에 중복·아이콘·남의 폴더를 거르고 뺀 개수를 말한다** |
| 개인정보 | **문서를 쓰기 전에 막는다.** ★ **재료를 빼는 게 아니라 값을 가린다** |
| 남의 저작물 | 위키로 안 엮는다. **재료에서 빼지도 않는다** |
| 절대경로 | 산출물에 안 적는다. 사용자 이름이 박혀 있다 |
| 위키 링크 | `[[대괄호]]` 를 안 쓴다. **상대경로 마크다운 링크**만 쓴다. 옵시디언에서만 열리면 06 이 못 쓴다 |
| 자동 커밋 | 스킬이 알아서 커밋하지 않는다 |
| 중복 | 묶은 뒤에 센다. 안 묶고 세면 개수가 틀린다 |
| NFC | 한글 파일명을 맞추고 나서 `grep` 한다. **스크립트의 그 줄을 지우지 않는다** |
| 갈래 크기 | 한 갈래에 몰리거나 너무 작으면 **그 자리에서 말한다.** 갈래를 다시 짜지 않고 **문서 쪽을 쪼갠다** |
| 손일 | **사람에게 안 넘긴다.** 내가 다 갈라놓고 사람은 짚는다 |
| 누가 하나 | **내가 할 일과 사람이 할 일을 갈라서 말한다.** 섞으면 사람이 아무것도 못 한다 |
| 어디에 넣었나 | 파일에 넣고 **어디에 넣었는지 한 줄로 말한다.** 안 말하면 못 찾는다 |
| 안 해도 되는 것 | **안 해도 된다고 먼저 말한다.** 그리고 안 하면 뭐가 없어지는지 |
| 「사용자가 직접」 | **산출물에 이렇게 적지 않는다.** 도구로 되는 일인지 먼저 본다 |
| 「나중에 채움」 | 산출물에 안 쓴다. 못 채웠으면 **왜 못 채웠는지**를 쓴다 |
| 꼬리 | **STEP 11 이 마지막이다.** 확인받는 자리 뒤에 아무것도 두지 않는다 |
| 클릭 | 다섯 개. 한 번에 하나씩. **실제 개수를 화면에 쓴다** |
| 순서 | 내부 상태를 첫 화면에 내지 않는다. 안내문이 먼저다 |
| 출력 | 목차와 목록을 화면에 쏟지 않는다. **묶음 단위로 접어서 낸다** |
| 시간 | **숫자로 약속하지 않는다** |
| 경로 | 이 파일에 적어두지 않는다. 설정에서 읽거나 사용자에게 묻는다 |
